import { ConvertDistPipe } from './convert-dist.pipe';

describe('ConvertDistPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDistPipe();
    expect(pipe).toBeTruthy();
  });
});
